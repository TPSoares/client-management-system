export const environment = {
  production: true,
  firebase: {
    apiKey: "your_credentials",
    authDomain: "your_credentials",
    projectId: "your_credentials",
    storageBucket: "your_credentials",
    messagingSenderId: "your_credentials",
    appId: "your_credentials",
    measurementId: "your_credentials"
  }
};
